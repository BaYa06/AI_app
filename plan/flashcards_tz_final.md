# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Импорт словаря из PDF
## Модуль извлечения и парсинга структурированных PDF-файлов

**Версия:** 1.0  
**Дата:** 14 января 2026  
**Приоритет:** КРИТИЧЕСКИЙ (Killer Feature)

---

## 🎯 НАЗНАЧЕНИЕ МОДУЛЯ

Автоматическое создание флеш-карточек из PDF-файлов со словарями, которые имеют структуру:
```
Немецкое слово – Русский перевод
Немецкое слово – Русский перевод
```

**Пример входного файла:**
```
Bar die, -s – Бар
Klavier das, -e – Фортепиано
Klavier spielen, hat gespielt – Играть на фортепиано
sammeln, hat gesammelt – Собирать, собрал
klassisch – Классический
```

---

## 📋 ТРЕБОВАНИЯ К ФУНКЦИОНАЛУ

### 1. ВЫБОР И ЗАГРУЗКА PDF

#### 1.1 Интерфейс выбора файла

**Экран "Импорт из PDF":**

```
┌─────────────────────────────────────┐
│ ←  Импорт из PDF                    │
├─────────────────────────────────────┤
│                                     │
│          📄                         │
│                                     │
│   Импортировать словарь             │
│        из PDF                       │
│                                     │
│  Загрузите PDF файл со словами      │
│  в формате: Слово – Перевод         │
│                                     │
│     [📎 Выбрать PDF файл]           │
│                                     │
│  ──────────────────────────────────│
│                                     │
│  ℹ️ Поддерживаемый формат:          │
│                                     │
│  ✓ Структурированные списки слов    │
│  ✓ Формат: слово – перевод          │
│  ✓ С артиклями и формами            │
│                                     │
│  📝 Пример:                         │
│  ┌───────────────────────────────┐  │
│  │ Bar die, -s – Бар             │  │
│  │ sammeln, hat gesammelt –      │  │
│  │ Собирать, собрал              │  │
│  │ klassisch – Классический      │  │
│  └───────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

**Требования:**
- Кнопка выбора файла открывает системный файловый менеджер
- Фильтр только PDF файлы
- Показать пример поддерживаемого формата
- Максимальный размер файла: 10 МБ
- Валидация: проверка что файл действительно PDF

#### 1.2 Техническая реализация выбора

```typescript
import DocumentPicker from 'react-native-document-picker';

interface PDFFile {
  uri: string;
  name: string;
  size: number;
  type: string;
}

async function selectPDFFile(): Promise<PDFFile | null> {
  try {
    const result = await DocumentPicker.pick({
      type: [DocumentPicker.types.pdf],
      copyTo: 'cachesDirectory' // Копируем в кеш для обработки
    });
    
    // Валидация размера
    if (result[0].size > 10 * 1024 * 1024) { // 10 МБ
      throw new Error('Файл слишком большой. Максимум 10 МБ');
    }
    
    return {
      uri: result[0].fileCopyUri || result[0].uri,
      name: result[0].name,
      size: result[0].size,
      type: result[0].type
    };
    
  } catch (error) {
    if (DocumentPicker.isCancel(error)) {
      return null; // Пользователь отменил
    }
    throw error;
  }
}
```

---

### 2. ИЗВЛЕЧЕНИЕ ТЕКСТА ИЗ PDF

#### 2.1 Библиотеки

**Для React Native:**
```bash
npm install react-native-pdf-lib
npm install react-native-fs
npm install @react-native-community/pdf
```

**Альтернатива (если нужна веб-версия):**
```bash
npm install pdf-parse
npm install pdfjs-dist
```

#### 2.2 Функция извлечения текста

```typescript
import RNFS from 'react-native-fs';
import { getInfoAsync } from 'react-native-pdf-lib';

/**
 * Извлекает весь текст из PDF файла
 * @param pdfUri - Путь к PDF файлу
 * @returns Извлеченный текст
 */
async function extractTextFromPDF(pdfUri: string): Promise<string> {
  try {
    // Читаем PDF как base64
    const base64 = await RNFS.readFile(pdfUri, 'base64');
    
    // Используем Claude API для извлечения текста
    const response = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'anthropic-version': '2023-06-01'
      },
      body: JSON.stringify({
        model: 'claude-sonnet-4-20250514',
        max_tokens: 4000,
        messages: [{
          role: 'user',
          content: [
            {
              type: 'document',
              source: {
                type: 'base64',
                media_type: 'application/pdf',
                data: base64
              }
            },
            {
              type: 'text',
              text: 'Extract all text from this PDF document. Return ONLY the raw text, no explanations, no formatting, just the text as it appears in the document.'
            }
          ]
        }]
      })
    });
    
    const data = await response.json();
    const extractedText = data.content[0].text;
    
    return extractedText;
    
  } catch (error) {
    console.error('PDF extraction error:', error);
    throw new Error('Не удалось извлечь текст из PDF');
  }
}
```

**Требования к извлечению:**
- ✅ Сохранять переносы строк
- ✅ Корректно обрабатывать UTF-8 (кириллица + латиница)
- ✅ Убирать лишние пробелы
- ✅ Сохранять знаки препинания (тире, запятые)
- ✅ Обработка ошибок с понятными сообщениями

#### 2.3 Экран загрузки

```
┌─────────────────────────────────────┐
│ ←  Обработка PDF                    │
├─────────────────────────────────────┤
│                                     │
│                                     │
│          ⏳                         │
│                                     │
│   Извлекаем текст из PDF...         │
│                                     │
│   ████████░░░░░░░░░░ 40%            │
│                                     │
│   Обработано 15 из 35 страниц       │
│                                     │
│                                     │
│   Это займет 5-15 секунд            │
│                                     │
└─────────────────────────────────────┘
```

---

### 3. ПАРСИНГ И СОЗДАНИЕ КАРТОЧЕК

#### 3.1 Алгоритм парсинга

**Поддерживаемые форматы строк:**

**Формат 1: Существительные с артиклем**
```
Bar die, -s – Бар
Museum das, Museen – Музей, музеи
Autor der, -en – Автор (муж.)
```

**Формат 2: Глаголы**
```
sammeln, hat gesammelt – Собирать, собрал
vor·schlagen, du schlägst vor, er schlägt vor, hat vorgeschlagen – Предлагать
```

**Формат 3: Прилагательные и другие слова**
```
klassisch – Классический
offen – Открытый
```

**Формат 4: Фразы**
```
Klavier spielen, hat gespielt – Играть на фортепиано
einen Spaziergang machen, hat gemacht – Совершать прогулку
```

#### 3.2 Функция парсинга

```typescript
interface FlashCard {
  front: string;        // Немецкое слово/фраза
  back: string;         // Русский перевод
  hint?: string;        // Подсказка (например, формы глагола)
  article?: string;     // Артикль (der/die/das)
  plural?: string;      // Форма множественного числа
  partII?: string;      // Partizip II для глаголов
  tags: string[];       // Теги для категоризации
  wordType: 'noun' | 'verb' | 'adjective' | 'phrase' | 'other';
}

/**
 * Парсит текст из PDF и создает карточки
 */
function parseGermanVocabulary(text: string): FlashCard[] {
  const cards: FlashCard[] = [];
  const lines = text.split('\n');
  let currentCategory = 'Без категории';
  
  for (let line of lines) {
    line = line.trim();
    
    // Пропускаем пустые строки
    if (!line) continue;
    
    // Определяем категорию (строки в скобках)
    // Например: "Freizeitaktivitäten (Свободное время)"
    const categoryMatch = line.match(/^(.+?)\s*\((.+?)\)\s*$/);
    if (categoryMatch) {
      currentCategory = categoryMatch[1].trim();
      continue;
    }
    
    // Пропускаем заголовки без тире
    if (!line.includes('–') && !line.includes('—') && !line.includes('-')) {
      continue;
    }
    
    // ПАТТЕРН 1: Существительное с артиклем
    // "Bar die, -s – Бар"
    const nounPattern = /^(.+?)\s+(der|die|das),?\s*([^–—-]+?)\s*[–—-]\s*(.+)$/;
    const nounMatch = line.match(nounPattern);
    
    if (nounMatch) {
      const [_, word, article, plural, translation] = nounMatch;
      
      cards.push({
        front: `${article} ${word.trim()}`,
        back: translation.trim(),
        article: article,
        plural: plural.trim() || undefined,
        hint: plural.trim() ? `Множ. число: ${plural.trim()}` : undefined,
        tags: [currentCategory, 'существительное'],
        wordType: 'noun'
      });
      continue;
    }
    
    // ПАТТЕРН 2: Глагол с формами
    // "sammeln, hat gesammelt – Собирать, собрал"
    const verbPattern = /^(.+?),\s*hat\s+(.+?)\s*[–—-]\s*(.+)$/;
    const verbMatch = line.match(verbPattern);
    
    if (verbMatch) {
      const [_, infinitive, partizip, translation] = verbMatch;
      
      cards.push({
        front: infinitive.trim(),
        back: translation.trim(),
        partII: partizip.trim(),
        hint: `Partizip II: hat ${partizip.trim()}`,
        tags: [currentCategory, 'глагол'],
        wordType: 'verb'
      });
      continue;
    }
    
    // ПАТТЕРН 3: Глагол с дополнительными формами
    // "vor·schlagen, du schlägst vor, er schlägt vor, hat vorgeschlagen"
    const verbComplexPattern = /^(.+?),\s*(.+?),\s*(.+?),\s*hat\s+(.+?)\s*[–—-]\s*(.+)$/;
    const verbComplexMatch = line.match(verbComplexPattern);
    
    if (verbComplexMatch) {
      const [_, infinitive, duForm, erForm, partizip, translation] = verbComplexMatch;
      
      cards.push({
        front: infinitive.trim(),
        back: translation.trim(),
        partII: partizip.trim(),
        hint: `${duForm.trim()}, ${erForm.trim()}, hat ${partizip.trim()}`,
        tags: [currentCategory, 'глагол', 'неправильный'],
        wordType: 'verb'
      });
      continue;
    }
    
    // ПАТТЕРН 4: Простое слово или фраза
    // "klassisch – Классический"
    const simplePattern = /^(.+?)\s*[–—-]\s*(.+)$/;
    const simpleMatch = line.match(simplePattern);
    
    if (simpleMatch) {
      const [_, word, translation] = simpleMatch;
      
      // Определяем тип слова
      let wordType: FlashCard['wordType'] = 'other';
      const tags = [currentCategory];
      
      // Если есть запятая в немецком слове - скорее всего фраза
      if (word.includes(',')) {
        wordType = 'phrase';
        tags.push('фраза');
      } else {
        // Простое прилагательное или наречие
        wordType = 'adjective';
        tags.push('прилагательное/наречие');
      }
      
      cards.push({
        front: word.trim(),
        back: translation.trim(),
        tags: tags,
        wordType: wordType
      });
    }
  }
  
  return cards;
}
```

#### 3.3 Валидация результатов парсинга

```typescript
/**
 * Проверяет качество распознанных карточек
 */
function validateParsedCards(cards: FlashCard[]): {
  valid: boolean;
  warnings: string[];
  stats: {
    total: number;
    nouns: number;
    verbs: number;
    other: number;
  };
} {
  const warnings: string[] = [];
  
  // Проверка 1: Минимальное количество карточек
  if (cards.length < 5) {
    warnings.push('Найдено слишком мало карточек. Проверьте формат PDF.');
  }
  
  // Проверка 2: Пустые поля
  const emptyFront = cards.filter(c => !c.front.trim()).length;
  const emptyBack = cards.filter(c => !c.back.trim()).length;
  
  if (emptyFront > 0) {
    warnings.push(`${emptyFront} карточек с пустой лицевой стороной`);
  }
  if (emptyBack > 0) {
    warnings.push(`${emptyBack} карточек с пустым переводом`);
  }
  
  // Проверка 3: Подозрительно короткие карточки
  const tooShort = cards.filter(c => c.front.length < 2 || c.back.length < 2).length;
  if (tooShort > cards.length * 0.1) { // Больше 10%
    warnings.push('Много подозрительно коротких карточек');
  }
  
  // Статистика
  const stats = {
    total: cards.length,
    nouns: cards.filter(c => c.wordType === 'noun').length,
    verbs: cards.filter(c => c.wordType === 'verb').length,
    other: cards.filter(c => !['noun', 'verb'].includes(c.wordType)).length
  };
  
  return {
    valid: warnings.length === 0,
    warnings,
    stats
  };
}
```

---

### 4. ПРЕДПРОСМОТР И РЕДАКТИРОВАНИЕ

#### 4.1 Экран предпросмотра

```
┌─────────────────────────────────────┐
│ ←  Проверка карточек                │
├─────────────────────────────────────┤
│                                     │
│  ✓ Импортировано 65 карточек        │
│                                     │
│  📊 Статистика:                     │
│  • Существительные: 28              │
│  • Глаголы: 22                      │
│  • Другое: 15                       │
│                                     │
│  ──────────────────────────────────│
│                                     │
│  [Все] [Существ.] [Глаголы] [Др.]  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ #1          Существительное   │  │
│  │                           ✏️ 🗑️│  │
│  │ Лицевая:                      │  │
│  │ die Bar                       │  │
│  │                               │  │
│  │ Обратная:                     │  │
│  │ Бар                           │  │
│  │                               │  │
│  │ 💡 Множ. число: -s            │  │
│  │ 🏷️ Freizeitaktivitäten         │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ #2                    Глагол  │  │
│  │                           ✏️ 🗑️│  │
│  │ Лицевая:                      │  │
│  │ sammeln                       │  │
│  │                               │  │
│  │ Обратная:                     │  │
│  │ Собирать, собрал              │  │
│  │                               │  │
│  │ 💡 Partizip II: hat gesammelt │  │
│  └───────────────────────────────┘  │
│                                     │
│  [еще 63 карточки...]             │
│                                     │
├─────────────────────────────────────┤
│ [Отмена]    [Сохранить все (65)]    │
└─────────────────────────────────────┘
```

**Требования:**
- ✅ Показать общую статистику
- ✅ Фильтры по типу слов
- ✅ Возможность редактировать каждую карточку
- ✅ Возможность удалить карточку
- ✅ Показать подсказки и теги
- ✅ Кнопки "Сохранить все" и "Отмена"

#### 4.2 Редактирование карточки

```
┌─────────────────────────────────────┐
│ ←  Редактировать карточку      ✓    │
├─────────────────────────────────────┤
│                                     │
│  Лицевая сторона *                  │
│  ┌───────────────────────────────┐  │
│  │ die Bar                       │  │
│  └───────────────────────────────┘  │
│                                     │
│  Обратная сторона *                 │
│  ┌───────────────────────────────┐  │
│  │ Бар                           │  │
│  └───────────────────────────────┘  │
│                                     │
│  Подсказка (опционально)            │
│  ┌───────────────────────────────┐  │
│  │ Множ. число: -s               │  │
│  └───────────────────────────────┘  │
│                                     │
│  Артикль                            │
│  ○ der   ● die   ○ das   ○ нет    │
│                                     │
│  Теги                               │
│  ┌───────────────────────────────┐  │
│  │ Freizeitaktivitäten, существ. │  │
│  └───────────────────────────────┘  │
│                                     │
│  ☐ Пометить как сложное слово       │
│                                     │
└─────────────────────────────────────┘
```

#### 4.3 Компонент карточки в списке

```typescript
interface CardPreviewProps {
  card: FlashCard;
  index: number;
  onEdit: (card: FlashCard) => void;
  onDelete: () => void;
}

const CardPreview: React.FC<CardPreviewProps> = ({
  card,
  index,
  onEdit,
  onDelete
}) => {
  const [isExpanded, setIsExpanded] = useState(false);
  
  return (
    <TouchableOpacity
      style={styles.cardPreview}
      onPress={() => setIsExpanded(!isExpanded)}
    >
      {/* Header */}
      <View style={styles.cardHeader}>
        <View style={styles.cardNumber}>
          <Text style={styles.numberText}>#{index + 1}</Text>
          <View style={styles.typeBadge}>
            <Text style={styles.typeText}>
              {card.wordType === 'noun' ? 'Существительное' : 
               card.wordType === 'verb' ? 'Глагол' : 'Другое'}
            </Text>
          </View>
        </View>
        
        <View style={styles.actions}>
          <TouchableOpacity onPress={() => onEdit(card)}>
            <Edit2 size={18} color="#6366F1" />
          </TouchableOpacity>
          <TouchableOpacity onPress={onDelete}>
            <Trash2 size={18} color="#EF4444" />
          </TouchableOpacity>
        </View>
      </View>
      
      {/* Content */}
      <View style={styles.cardContent}>
        <View style={styles.side}>
          <Text style={styles.label}>Лицевая:</Text>
          <Text style={styles.text}>{card.front}</Text>
        </View>
        
        {isExpanded && (
          <>
            <View style={styles.side}>
              <Text style={styles.label}>Обратная:</Text>
              <Text style={styles.text}>{card.back}</Text>
            </View>
            
            {card.hint && (
              <Text style={styles.hint}>💡 {card.hint}</Text>
            )}
            
            {card.tags.length > 0 && (
              <View style={styles.tags}>
                {card.tags.map((tag, i) => (
                  <View key={i} style={styles.tag}>
                    <Text style={styles.tagText}>{tag}</Text>
                  </View>
                ))}
              </View>
            )}
          </>
        )}
      </View>
    </TouchableOpacity>
  );
};
```

---

### 5. СОХРАНЕНИЕ В БАЗУ ДАННЫХ

#### 5.1 Создание набора из импорта

```typescript
/**
 * Сохраняет импортированные карточки в новый набор
 */
async function saveImportedCards(
  cards: FlashCard[],
  sourceFileName: string
): Promise<string> {
  // Генерируем название набора из имени файла
  const setName = sourceFileName
    .replace('.pdf', '')
    .replace(/_/g, ' ')
    .trim();
  
  // Создаем новый набор
  const setId = generateUUID();
  const now = Date.now();
  
  const cardSet: CardSet = {
    id: setId,
    userId: getCurrentUserId(),
    title: setName,
    description: `Импортировано из ${sourceFileName}`,
    category: 'Немецкий язык',
    tags: ['импорт', 'словарь'],
    createdAt: now,
    updatedAt: now,
    isPublic: false,
    cardCount: cards.length
  };
  
  // Сохраняем набор
  await database.sets.insert(cardSet);
  
  // Сохраняем карточки
  for (const card of cards) {
    const cardId = generateUUID();
    
    const dbCard: Card = {
      id: cardId,
      setId: setId,
      frontText: card.front,
      backText: card.back,
      frontImage: null,
      backImage: null,
      frontAudio: null,
      backAudio: null,
      hint: card.hint,
      tags: card.tags,
      createdAt: now,
      updatedAt: now,
      
      // SRS параметры (начальные значения)
      easeFactor: 2.5,
      interval: 0,
      repetitions: 0,
      nextReviewDate: now, // Доступна для изучения сразу
      lastReviewDate: null
    };
    
    await database.cards.insert(dbCard);
  }
  
  return setId;
}
```

#### 5.2 Экран успешного сохранения

```
┌─────────────────────────────────────┐
│                                     │
│          ✅                         │
│                                     │
│   Успешно импортировано!            │
│                                     │
│   65 карточек добавлено в набор     │
│   "A2.2 Wörter L8"                  │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ Начать изучение сейчас      │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ Перейти к набору            │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ Импортировать еще один PDF  │    │
│  └─────────────────────────────┘    │
│                                     │
│  [Вернуться на главную]             │
│                                     │
└─────────────────────────────────────┘
```

---

### 6. ОБРАБОТКА ОШИБОК

#### 6.1 Возможные ошибки и решения

**Ошибка 1: Файл не PDF**
```
❌ Неверный формат файла
Пожалуйста, выберите PDF файл
```

**Ошибка 2: Файл слишком большой**
```
❌ Файл слишком большой
Максимальный размер: 10 МБ
Размер вашего файла: 15.2 МБ
```

**Ошибка 3: Не удалось извлечь текст**
```
❌ Не удалось прочитать PDF
Возможно, файл поврежден или защищен паролем
```

**Ошибка 4: Нет подходящих карточек**
```
⚠️ Не найдено карточек
PDF не содержит текст в формате: слово – перевод

Пример правильного формата:
Bar die, -s – Бар
sammeln, hat gesammelt – Собирать

[Попробовать другой файл]
```

**Ошибка 5: Мало карточек**
```
⚠️ Найдено только 3 карточки
Возможно, формат PDF не распознан правильно

Хотите продолжить или выбрать другой файл?

[Продолжить]  [Выбрать другой]
```

#### 6.2 Код обработки ошибок

```typescript
async function handlePDFImport(fileUri: string): Promise<ImportResult> {
  try {
    // Шаг 1: Извлечение текста
    updateProgress('Извлекаем текст из PDF...', 30);
    const text = await extractTextFromPDF(fileUri);
    
    if (!text || text.trim().length < 50) {
      return {
        success: false,
        error: 'PDF не содержит достаточно текста'
      };
    }
    
    // Шаг 2: Парсинг
    updateProgress('Создаем карточки...', 60);
    const cards = parseGermanVocabulary(text);
    
    if (cards.length === 0) {
      return {
        success: false,
        error: 'Не найдено карточек в нужном формате'
      };
    }
    
    if (cards.length < 5) {
      return {
        success: true,
        cards: cards,
        warning: `Найдено только ${cards.length} карточек. Проверьте результат.`
      };
    }
    
    // Шаг 3: Валидация
    updateProgress('Проверяем качество...', 90);
    const validation = validateParsedCards(cards);
    
    updateProgress('Готово!', 100);
    
    return {
      success: true,
      cards: cards,
      warnings: validation.warnings,
      stats: validation.stats
    };
    
  } catch (error) {
    console.error('Import error:', error);
    
    return {
      success: false,
      error: error.message || 'Неизвестная ошибка при импорте'
    };
  }
}

interface ImportResult {
  success: boolean;
  cards?: FlashCard[];
  error?: string;
  warning?: string;
  warnings?: string[];
  stats?: {
    total: number;
    nouns: number;
    verbs: number;
    other: number;
  };
}
```

---

### 