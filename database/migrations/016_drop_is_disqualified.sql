-- Миграция 016: удаление неиспользуемого столбца test_participants.is_disqualified
--
-- Столбец был заведён под будущую фичу дисквалификации за читерство, но нигде не читался и не
-- писался — ни на бэкенде (api/test.js), ни на клиенте. Реализация самой фичи (что считать
-- читерством, как это показывать учителю, обнуляется ли счёт) — отдельное продуктовое решение,
-- которое не принималось; заводить его неявно вместе с чисткой мёртвого кода не стали.
-- См. план, пункт 46.

ALTER TABLE test_participants DROP COLUMN IF EXISTS is_disqualified;
